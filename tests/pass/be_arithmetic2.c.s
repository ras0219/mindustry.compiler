_f:
    subq $56, %rsp
    mov %rdi, 0(%rsp)
    mov %esi, 8(%rsp)
    movl $1, 12(%rsp)
    mov 12(%rsp), %eax
    shl $1, %rax
    mov %rax, 24(%rsp)
    mov 24(%rsp), %r11d
    mov %r11d, 12(%rsp)
    mov 12(%rsp), %eax
    shl $1, %rax
    mov %rax, 24(%rsp)
    mov 24(%rsp), %r11d
    mov %r11d, 12(%rsp)
    mov 12(%rsp), %eax
    shr $1, %rax
    mov %rax, 24(%rsp)
    mov 24(%rsp), %r11d
    mov %r11d, 12(%rsp)
    mov 12(%rsp), %eax
    shr $1, %rax
    mov %rax, 24(%rsp)
    mov 24(%rsp), %r11d
    mov %r11d, 12(%rsp)
    movl $1, 16(%rsp)
    mov 12(%rsp), %eax
    mov 16(%rsp), %ecx
    shlq %cx, %rax
    mov %rax, 24(%rsp)
    mov 24(%rsp), %r11d
    mov %r11d, 12(%rsp)
    mov 12(%rsp), %eax
    mov 16(%rsp), %ecx
    shlq %cx, %rax
    mov %rax, 24(%rsp)
    mov 24(%rsp), %r11d
    mov %r11d, 12(%rsp)
    mov 12(%rsp), %eax
    mov 16(%rsp), %ecx
    shrq %cx, %rax
    mov %rax, 24(%rsp)
    mov 24(%rsp), %r11d
    mov %r11d, 12(%rsp)
    mov 12(%rsp), %eax
    mov 16(%rsp), %ecx
    shrq %cx, %rax
    mov %rax, 24(%rsp)
    mov 24(%rsp), %r11d
    mov %r11d, 12(%rsp)
    addq $56, %rsp
    ret