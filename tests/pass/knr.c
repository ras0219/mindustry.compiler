int compress2(dest, destLen, source, sourceLen, level)
char* dest;
long* destLen;
const char* source;
long sourceLen;
int level;
{
    return level;
}

long compress3(a, b)
char* b;
long a;
{
    return a;
}

long compress4(a, b)
int a, b;
{
    return b + a;
}
